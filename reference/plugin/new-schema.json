{
  "type": "object",
  "properties": {
    "program": {
      "type": "string",
      "enum": ["myapp"],
      "description": "Program to execute after validation"
    },
    "files": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "Name of the downloaded files to read and print"
      }
    }
  },
  "required": ["program", "files"]
}
